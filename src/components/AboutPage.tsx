import { SobreSection } from "./SobreSection";

export const AboutPage = () => {
  return (
    <main className="pt-20">
      {/* Esta página simplesmente renderiza a seção "Sobre" que já criamos */}
      <SobreSection />
    </main>
  );
};
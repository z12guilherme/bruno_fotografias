import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.brunonascimento.fotografia',
  appName: 'Bruno Nascimento Fotografia',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
